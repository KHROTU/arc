# URLs for webfetch and websearch tool testing

## webfetch URLs
1. https://example.com — Simple HTML page (should return raw HTML)
2. https://httpbin.org/html — Structured HTML with known content
3. https://httpbin.org/json — JSON response
4. https://httpbin.org/status/404 — Deliberate 404 for error handling
5. https://this-domain-does-not-exist-arc-test.invalid — DNS failure for error handling

## web.search queries
Use `web.search` with these terms to verify the search tool works:
1. "Arc agentic coding assistant VS Code extension" — should find the Arc extension
2. "TypeScript type narrowing best practices 2025" — programming topic
3. "REPLACE_ME in code search" — should return few/no results, verifying search handles rare queries
